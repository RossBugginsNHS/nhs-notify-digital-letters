$schema: https://json-schema.org/draft/2020-12/schema
title: PDMResourceRetriesExceeded
type: object
allOf:
  - $ref: ../digital-letters-pdm-profile.schema.yaml
properties:
  type:
    type: string
    const: uk.nhs.notify.digital.letters.pdm.resource.retries.exceeded.v1
    description: Concrete versioned event type string for this example event (.vN suffix).
  source:
    type: string
    pattern: ^/nhs/england/notify/(production|staging|development|uat)/(primary|secondary|dev-[0-9]+)/data-plane/digitalletters/pdm
    description: Event source for digital letters  examples.

  dataschema:
    type: string
    const: ../data/digital-letters-pdm-resource-retries-exceeded-data.yaml
    description: Canonical URI of the example event's data schema.
    examples:
      - digital-letters-pdm-resource-retries-exceeded-data.yaml
  data:
    $ref: ../data/digital-letters-pdm-resource-retries-exceeded-data.schema.yaml
